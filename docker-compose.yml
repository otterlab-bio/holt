services:
  holt-build:
    build:
      context: ./holt-build
    image: ghcr.io/otterlab-bio/holt-build:latest
    container_name: holt-build
    stdin_open: true
    tty: true

  holt-run:
    build:
      context: ./holt-run
    image: ghcr.io/otterlab-bio/holt-run:latest
    container_name: holt-run
    ports:
      - "2222:2222"
      - "8889:8889"
      - "8080:8080"
      - "8787:8787"
    stdin_open: true
    tty: true
